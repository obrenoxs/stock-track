package com.stocktrack.auditlog.enums;

public enum ActionType {
    CREATED,
    LOANED,
    RETURNED,
    SENT_TO_MAINTENANCE,
    RETURNED_FROM_MAINTENANCE,
    DISCARDED,
    LOCATION_CHANGED,
    EDITED
}
